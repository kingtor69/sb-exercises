describe('refactoring obj into iObject', () => {
    it('should return the same results', () => {
        // expect(iObject).toEqual(obj);
        // expect(aay).toEqual(a);
        // expect(bee).toEqual(b);
    })
})